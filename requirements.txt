# FootballVision Dependencies
# ============================================
# ALL DEPENDENCIES ARE FREE AND OPEN SOURCE
# No API keys or paid services required
# ============================================

# Core ML/CV (FREE - Apache/BSD licensed)
ultralytics>=8.0.0          # YOLOv8 detection
opencv-python>=4.8.0        # Image/video processing
numpy>=1.24.0               # Array operations
torch>=2.0.0                # PyTorch models

# macOS Screen Capture (FREE - MIT licensed)
pyobjc-framework-Quartz; sys_platform == 'darwin'  # Quartz/CoreGraphics APIs

# Tracking (FREE - MIT licensed). 0.28 deprecates the ByteTrack API used by
# both this engine and the pinned Roboflow Sports revision.
supervision>=0.27.0,<0.28.0 # ByteTrack + utilities
# Pitch geometry, view transforms, and team classifier used by fv_engine.py.
sports @ git+https://github.com/roboflow/sports.git@42c80c06b6b65a7f89455b89fe31cdf4c38ba227

# Primary web server (FREE - BSD licensed)
fastapi==0.141.1            # Async web framework + WebSockets
starlette==1.6.0            # Template API used by ui/api.py
uvicorn[standard]>=0.29.0   # ASGI server
python-multipart>=0.0.9     # Form/file upload support
jinja2>=3.1.0               # HTML templating

# Database (FREE - MIT licensed)
aiosqlite>=0.20.0           # Async SQLite

# Visualization (FREE - PSF/BSD licensed)
matplotlib>=3.7.0           # Heatmap rendering
pandas>=2.0.0               # Tracking/metric tables and analyst reports

# Evaluation (FREE - BSD licensed)
scipy>=1.10.0               # Statistical functions + gaussian filter
scikit-learn>=1.3.0         # Metrics utilities

# Static post-match analyst report (scripts/analyst_report.py). Pinned so the
# generated report layout remains stable across clean installs.
mplsoccer==1.4.0
seaborn>=0.13

# Model bootstrap script
gdown>=5.2.0

# Retained for the legacy upload UI in ui/web_app.py.
flask>=3.0.0
